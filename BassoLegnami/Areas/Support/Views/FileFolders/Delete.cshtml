@model BassoLegnami.Model.Models.Support.FileFolder
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<SharedResource> SR

@{
    ViewData["Title"] = string.Format("{0} {1}", SR["Delete"].Value, Html.DisplayNameFor(model => model.FileFolderID).ToString());
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        <div class="card stacked-form">
            <div class="card-header">
                <h4 class="card-title title">@ViewData["Title"]</h4>
            </div>
            <div class="card-body">
                <form asp-action="Delete">
                    <hr />
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <display asp-for="Name" class="form-control"></display>
                    </div>
                    <div class="form-group">
                        <label asp-for="Key" class="control-label"></label>
                        <display asp-for="Key" class="form-control"></display>
                    </div>
                    <div class="form-group">
                        <label asp-for="Path" class="control-label"></label>
                        <display asp-for="Path" class="form-control"></display>
                    </div>
                    <hr />
                    <div class="text-right">
                        <input type="hidden" asp-for="FileFolderID" />
                        <button type="submit" value="Delete" class="btn btn-danger"><i class="fa fa-trash"></i> @SR["Delete"]</button>
                        <a asp-action="Index" class="btn btn-default"><i class="fa fa-chevron-left"></i> @SR["BackToList"]</a>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>
</div>
